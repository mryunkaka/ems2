<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(?:storage|backup|logs)/.*\.(?:php|phtml|phar|php\d*)$ - [F,L,NC]
</IfModule>

<FilesMatch "(?i)\.(php|phtml|phar|php\d*)$">
    Require all denied
</FilesMatch>
